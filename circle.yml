## Customize the test machine
machine:
  java:
    version: oraclejdk8
    
dependencies:
  override: 
    - mvn test

general:
  branches:
    only:
      - master # list of branches to build
    

    