## Customize the test machine
machine:
  java:
    version: oraclejdk8
    
dependencies:
  override:
    - mvn clean 
    - mvn test-compile

general:
  branches:
    only:
      - master # list of branches to build
    

    